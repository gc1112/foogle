<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>edit recipe</title>
    <link href="/style.css" rel="stylesheet">
</head>

<body>
    <%- include ('partial/nav') %>
        <div class="container">
            <br>
            <div class="text-center">
                <h2>Add New product</h2>
            </div>
            <br>
            <div class="text-center">
                <form action="/recipe_edit/<%= data[0].i_id %>" method="POST" enctype="multipart/form-data">
                    <label for="name">name:</label><br>
                    <input type="text" name="name" placeholder="<%= data[0].name %>"  required><br><br>
                    <label for="description">description:</label><br>
                    <textarea name="description" rows="2" cols="50" placeholder="<%= data[0].description %>" required></textarea><br>
                    <label for="step">steps:</label><br>
                    <textarea name="step" rows="4" cols="50" placeholder="<%= data[0].step %>" required></textarea><br>
                    <label for="ingredient">ingredients:</label><br>
                    <textarea name="step" rows="4" cols="50" placeholder="<%= data[0].i_name %>" required></textarea>                   
                    <div class="txt">please seperate each step with a line '|' </div><br><br>
                    <button type="submit">Add product</button>
                </form>
            </div>
        </div>
</body>

</html>